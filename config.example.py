"""Server-side configuration template — copy to config.py and edit before running."""

# Admin users: {username: password}
ADMIN_USERS = {
    "admin": "change-me",
}

# Session cookie lifetime (seconds) — default 8 hours
SESSION_TTL = 8 * 3600
